import { defineConfig } from 'astro/config';

export default defineConfig({
  output: 'static',
  // TODO: 部署后改为你的实际 pages.dev 地址，例如 https://asset-rental-platform.pages.dev
  site: 'https://asset-rental-platform.pages.dev',
});
